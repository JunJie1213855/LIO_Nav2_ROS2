# global_relocalization

早期的全局重定位节点，现已被 `global_relocalization_kiss_matcher` 取代。

## 特点

- 通过 FetchContent 从 GitHub 拉取 `small_gicp` 并编译
- 功能上与 `global_relocalization_kiss_matcher` 类似，但不集成 KISS-Matcher

## 当前状态

保留用于参考和测试用途。推荐使用 `global_relocalization_kiss_matcher` 作为重定位方案。
